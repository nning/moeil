user = mail
password = foobar
hosts = localhost
dbname = mail
query = select username from mailboxes where username = '%u' and active = true and domain_id in (select id from domains where name = '%d')
