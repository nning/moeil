%h1 Mailboxes
%hr

%p
  Got
  = pluralize(collection.count, 'mailbox')

%table.table.table-striped
  %thead
    %th E-Mail
    %th sha512-crypt?
    %th Admin?
    %th
  %tbody
    = render collection
