= render partial: 'shared/welcome'

.span6
  %h2 Login
  %hr

  = simple_form_for resource, url: mailbox_session_path do |f|
    = f.input :email, label: 'E-Mail', autofocus: true
    = f.input :password
    .control-group.controls
      = f.button :submit, 'Login', class: 'btn-primary'
