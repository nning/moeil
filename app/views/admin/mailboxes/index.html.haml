%h1
  Mailboxes for
  = parent.name
.btn-toolbar
  .btn-group
    = link_to_back
    = link_to_create [:new, :admin, parent, :mailbox]
%hr

%p
  Got
  = pluralize(collection.count, 'mailbox')

%table.table.table-striped
  %thead
    %th E-Mail Address
    %th Admin?
    %th Active?
    %th
  %tbody
    = render collection
